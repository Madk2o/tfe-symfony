{% extends 'base.html.twig' %}

{% block title %}Rock & Roule{% endblock %}

{% block body %}
    <h1 class="font-monospace text-center bg-dark text-warning m-0 p-0">Ajout nouveau</h1>

    <div class="d-flex justify-content-between flex-column m-1">
        <div class="d-flex justify-content-center align-content-between p-1 m-1">
            {{ include('tracks/_form.html.twig') }}
        </div>

        <div class="d-flex justify-content-center m-1">
            <a href="{{ path('tracks_index') }}" class="btn btn-primary justify-content-center">Retour liste</a>
        </div>
    
    </div>

    

    
{% endblock %}
