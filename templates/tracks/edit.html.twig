{% extends 'base.html.twig' %}

{% block title %}Rock & Roule{% endblock %}

{% block body %}
    <h1 class="text-center font-monospace text-warning">Editer Tracks</h1>
    <div class="d-flex justify-content-center p-2">
        {{ include('tracks/_form.html.twig', {'button_label': 'Update'}) }}
    </div>
    <div class="d-flex justify-content-center m-2">
        <a href="{{ path('tracks_index') }}" class="btn btn-primary m-1">Retour liste</a>
        {{ include('tracks/_delete_form.html.twig') }}
    </div>
    
{% endblock %}
