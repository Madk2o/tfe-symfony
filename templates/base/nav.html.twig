    <nav class="navbar navbar-expand-lg bg-dark alert font-monospace m-2 mb-4">
        <div class="container-fluid">
                    <a class="navbar-brand img rotate-center" href="{{ path('artists_index') }}"><img src="{{ asset('images/Rock&Roule4.jpg') }}"/></a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
            <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                <div class="navbar-nav">                    
                    <a class="nav-link active text-warning" href="{{ path('artists_index') }}">Artistes</a>
                    <a class="nav-link active text-warning" href="{{ path('albums_index') }}">Albums</a>
                    <a class="nav-link active text-warning" href="{{ path('tracks_index') }}">Tracks</a>
                    <a class="nav-link active text-warning" href="{{ path('genres_index') }}">Genres</a>
                    <a class="nav-link active text-warning" href="{{ path('playlists_index') }}">Playlists</a>
                    <a class="nav-link active text-warning" href="{{ path('media_types_index') }}">Media_types</a>
                </div>
            </div>
        </div>
    </nav>