{% extends 'base.html.twig' %}

{% block title %}Rock & Roule{% endblock %}

{% block body %}
    <h1 class="font-monospace text-center bg-dark text-warning m-0 p-0">Ajout playlist</h1>

<div class="d-flex justify-content-center flex-column m-1">
    <div class="d-flex justify-content-center flex-column m-1">
        {{ include('playlists/_form.html.twig') }}
    </div>
    <div class="d-flex justify-content-center m-1">
        <a href="{{ path('playlists_index') }}" class="btn btn-primary m-1">Retour liste</a>
    </div>
</div>

    

  
{% endblock %}
