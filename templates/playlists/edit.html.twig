{% extends 'base.html.twig' %}

{% block title %}Rock & Roule{% endblock %}

{% block body %}
    <h1 class="text-center text-warning font-monospace">Editer Playlists</h1>
    
    <div class="d-flex justify-content-center m-2">
    
                {{ include('playlists/_form.html.twig', {'button_label': 'Update'}) }}
                <div class="d-flex justify-content-center">

    </div>
    </div>

    <div class="d-flex justify-content-center m-2">
        <a href="{{ path('playlists_index') }}" class="btn btn-primary m-1">Retour Liste</a>

        {{ include('playlists/_delete_form.html.twig') }}
    </div>

    



{% endblock %}
