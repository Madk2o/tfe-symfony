<form method="post" action="{{ path('genres_delete', {'genreid': genre.genreid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ genre.genreid) }}">
    <button class="btn btn-danger m-1">Effacer</button>
</form>
