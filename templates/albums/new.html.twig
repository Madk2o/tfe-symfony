{% extends 'base.html.twig' %}

{% block title %}Nouvel Album{% endblock %}

{% block body %}

    <h1 class="font-monospace text-center bg-dark text-warning m-0 p-0">Ajout Nouveau</h1>

    <div class="d-flex justify-content-center text-warning flex-column m-1 p-1">
        <div class="d-flex justify-content-center">
            {{ include('albums/_form.html.twig') }}
        </div>
        <div class="d-flex justify-content-center m-1">
            <a href="{{ path('albums_index') }}" class="btn btn-primary justify-content-center">Retour Liste</a>
        </div>
    </div>
    
    

    
{% endblock %}
