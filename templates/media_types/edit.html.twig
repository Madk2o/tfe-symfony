{% extends 'base.html.twig' %}

{% block title %}Rock & Roule{% endblock %}

{% block body %}
    <h1 class="text-center font-monospace text-warning">Editer MediaTypes</h1>

    <div class="d-flex justify-content-center p-2">
        {{ include('media_types/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <div class="d-flex justify-content-center align-item-center">
        <a href="{{ path('media_types_index') }}" class="btn btn-primary m-1">Back to list</a>
        {{ include('media_types/_delete_form.html.twig') }}
    </div>


{% endblock %}
