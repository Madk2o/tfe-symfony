{% extends 'base.html.twig' %}

{% block title %}Rock & Roule{% endblock %}

{% block body %}

    <h1 class="text-center font-monospace text-warning">Artistes</h1>

    <table class="table table-striped text-light font-monospace">
        <tbody >
            <tr>
                <th class="text-warning">Numéro</th>
                <td class="text-light">{{ artist.artistid }}</td>
            </tr>
            <tr>
                <th class="text-warning">Nom</th>
                <td class="text-light">{{ artist.name }}</td>
            </tr>
        </tbody>
    </table>
    <div class="d-flex flex-row justify-content-center">
        <a href="{{ path('artists_index') }}" class="btn btn-primary m-1">Retour liste</a>

        <a href="{{ path('artists_edit', {'artistid': artist.artistid}) }}" class="m-1 btn btn-success">Editer</a>

        {{ include('artists/_delete_form.html.twig') }}
    </div>
{% endblock %}
