{% extends 'base.html.twig' %}

{% block title %}Rock & Roule{% endblock %}

{% block body %}
    <h1 class="text-center text-warning font-monospace">Editer Artistes</h1>
    <div class="d-flex justify-content-center p-2">
        {{ include('artists/_form.html.twig', {'button_label': 'Update'}) }}
    </div>
    <div class="d-flex justify-content-center m-2">
        <a href="{{ path('artists_index') }}" class="btn btn-primary m-1">Retour Liste</a>
        {{ include('artists/_delete_form.html.twig') }}
    </div>
{% endblock %}
